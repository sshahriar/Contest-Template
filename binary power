
ll binpow(ll n,ll x ) {
    if(x==0) return 1; 
    if(x&1) {
        ll ans = binExpo(n,x-1)%mod;
        return (n*ans )%mod ; 

    } else {
        ll ans = binExpo(n , x/2)%mod ;
        return (ans*ans)%mod ; 

    }

}
